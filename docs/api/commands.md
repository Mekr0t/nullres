# Commands

One function per command. Each computes and returns a
[result object](results.md); none of them print.

::: nullres.api
    options:
      members:
        - run
        - audit
        - budget
        - robust
        - sweep
        - ablate
        - xsec
        - feature_importance
        - fetch
        - ledger
        - resolve_universe
        - verify_panel
        - killed_warning
